# frozen_string_literal: true

# Matrix gemfile: proves the gemspec's `rails >= 7.1` claim on Rails 8.0.
# Select with BUNDLE_GEMFILE=gemfiles/rails_8.0.gemfile.

source 'https://rubygems.org'

gem 'rails', '~> 8.0.0'

eval_gemfile File.expand_path('common.rb', __dir__)

gemspec path: '..'
