import { Controller } from "@hotwired/stimulus"

// senren--select
// v0.1: a no-op pass-through. Future versions will swap a native select for a
// listbox-style component while preserving form submission semantics.
export default class extends Controller {
  connect() {
    // ready
  }
}
